# Copy to deploy.env and fill in your values
# deploy.env is gitignored — never commit it

GCP_PROJECT=your-gcp-project
GCP_REGION=us-east1
AR_REPO=vicinity
INSTANCE=vicinity-prod-vm
ZONE=us-east1-c
MACHINE=e2-standard-4
VM_TAG=vicinity-prod