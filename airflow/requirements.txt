# ── Pipeline Dependencies (Airflow Container) ────────────────
# Pinned to exact versions from the project venv (conflict-resolved).
# Airflow 2.10.4 ships its own SQLAlchemy, click, Jinja2, etc.
# — those are re-pinned LAST in the Dockerfile to stay Airflow-safe.

# Snowflake
snowflake-connector-python==4.4.0
snowflake-sqlalchemy==1.9.0

# HTTP / async
httpx==0.28.1
httpcore==1.0.9
anyio==4.13.0

# Data
pandas==2.3.3
numpy==2.4.4

# Config / env
python-dotenv==1.2.2
PyYAML==6.0.3

# Validation / settings
pydantic==2.12.5
pydantic-core==2.41.5
pydantic-settings==2.13.1

# Logging
structlog==25.5.0

# LLM (classifier)
openai==2.30.0
pinecone==8.1.2

# Listings
homeharvest==0.8.18
scrapling[fetchers]==0.4.5

# Airflow extras
apache-airflow-providers-slack
apache-airflow-providers-celery
redis==5.0.0

boto3==1.42.85